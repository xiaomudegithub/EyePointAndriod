<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mokuryuu.login">

    <uses-permission android:name="android.permission.INTERNET"
        tools:ignore="WrongManifestParent" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity android:name=".LoginMainActivity"></activity>
        <activity android:name=".ActivityLoginRegisterResetpasswdActivity"></activity>
        <activity android:name=".ValCodeActivity"></activity>
        <activity android:name=".HomeRootActivity">

        </activity>
        <activity android:name=".ActivityTabMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>